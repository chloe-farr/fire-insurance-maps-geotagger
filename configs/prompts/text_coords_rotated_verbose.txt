This is a fire insurance map of Victoria B.C. in 1895. It has been rotated so you can read labels that were not horizontal on the original scan. Detect and transcribe as much text as you can (words, numbers, abbreviations). Return the text with coordinates for each item you identify (e.g. bounding boxes in the model's usual coordinate format).
